import { RGB, HSL, Hex, HSV } from "../../types.js";
export declare function isValidRGB(input: RGB): boolean;
export declare function isValidHex(input: Hex): boolean;
export declare function isValidHSL(input: HSL): boolean;
export declare function isValidHSV(input: HSV): boolean;
